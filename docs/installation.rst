.. _installation:

Installation
============

* To install ::

    pip install django-site-access

* Add ``'django-site-access'`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # other apps
        "site_access",
    )

